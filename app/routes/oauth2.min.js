var express=require("express"),oauth2=express.Router(),bodyParser=require("body-parser"),oauthserver=require("oauth2-server");oauth2.use(bodyParser.urlencoded({extended:true}));oauth2.use(bodyParser.json());oauth2.oauth=oauthserver({model:{},grants:["password"],debug:true});oauth2.all("/oauth/token",app.oauth.grant());oauth2.get("/",function(c,a,b){a.send("Secret area")});oauth2.use(oauth2.oauth.errorHandler());